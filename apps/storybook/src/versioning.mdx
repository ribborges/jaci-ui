import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Guides/Versioning" />

# Versioning

Changesets is the source of truth for versions. The candidate is published with:

```sh
pnpm changeset pre enter rc
pnpm release:version
npm publish --tag rc
```
